// The smallest thing that proves the zone is reachable through the rewrite from
// www.crimetimesnacks.com. Kept deliberately dull; it is a probe, not a page.
export const dynamic = "force-dynamic";

export default function Health() {
  return <p>community zone ok</p>;
}
